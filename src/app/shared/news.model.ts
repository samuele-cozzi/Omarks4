import { Timestamp } from '@firebase/firestore-types';

export class News {
    author: string;
    content: string;
    description: string;
    publishedAt: string;
    title: string;
    url: string;
    urlToImage: string;
}
